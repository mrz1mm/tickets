/**
 * Rappresenta un dipartimento a cui un ticket può essere assegnato.
 * Corrisponde al DTO `DepartmentDto` del backend.
 */
export interface Department {
  id: number;
  name: string;
  description: string;
}
